//Palindrome?
// 07/04/2026 | 

        String name = "madam";
        String reverseName = "";
         
        for(int i=name.length()-1;i>=0;i--)
        {
          reverseName += name.charAt(i); 
        }

        if(name.equals(reverseName))
            System.out.println("Palindrome"); 
        else
            System.out.println("non-Palindrome"); 
        
   
============================================================================== 
// 07/04/2026 | 
Print all positive 

    int arr[] = {-10,-20,-30,0,10,20,30};
    
    for(int i=0;i<arr.length;i++)
        if(arr[i]<0)  
        arr[i] = - (arr[i]); //  - ( -10 ) = 10
        
    for(int k=0;k<arr.length;k++)
         System.out.print(arr[k]+" ");

============================================================================== 

//*Print 1st and 2nd repitative char with count.

        String name = "im deepak QA engineer";
        name = name.replaceAll("\\s+","");
        
        Map<Character, Integer> map = new LinkedHashMap<>();
        for(int i=0;i<name.length();i++){
            char c = name.charAt(i);
            
            if(map.containsKey(c))
                map.put(c,map.get(c)+1);
            else
                map.put(c,1);
        }
        int maxCount = 0, smaxCount = 0;
        char maxChar = ' ', smaxChar = ' ';
 
        for(Map.Entry<Character, Integer> entry : map.entrySet())
        {
            if(entry.getValue()>maxCount){
                smaxCount = maxCount;
                smaxChar = maxChar;
                
                maxCount = entry.getValue();
                maxChar = entry.getKey();
            }
             else if (entry.getValue() > smaxCount && entry.getValue() < maxCount) {
                smaxCount = entry.getValue();
                smaxChar = entry.getKey();
            }
        }
        System.out.println("Second most repeating character: " + smaxChar + " = " + smaxCount);
        System.out.println("Most repeating character: " + maxChar + " = " + maxCount);
==============================================================================
07/04/2026 | 
//*Print Missing Number 

        int arr[] = {12, 14, 18};
        int missingNu = 0;

        int diff = Integer.MAX_VALUE;
        for(int i=0;i<arr.length-1;i++)
        {
           diff = Math.min(diff,arr[i+1]-arr[i]);
        }

        for (int i = 0; i < arr.length - 1; i++) {

            if (arr[i + 1] - arr[i] != diff) {
                missingNu = arr[i] + diff;
                break;
            }
        }

        System.out.println(missingNu);

==============================================================================
07/04/2026 | 
// frequency of characters in a string "aabdddess"
Output = {a=2, b=1, d=3, e=1, s=2}

import java.util.LinkedHashMap;
import java.util.Map;

        String str = "aabdddess";
        
        Map<Character,Integer> map = new LinkedHashMap<>();
        for(int i=0;i<str.length();i++)
        {
            char c = str.charAt(i);
            if(map.containsKey(c))
            {
                map.put(c,map.get(c)+1);
            }
            else
            map.put(c,1);
        }
         
           for(Map.Entry<Character, Integer> entry : map.entrySet())
           {
               System.out.println(entry.getKey() +" -> "+entry.getValue());
           }

==============================================================================
06/04/2026 /
//frequency of only word Big and black

        String str = "Big black bug bit a big black dog on his big black nose";

        String[] words = str.toLowerCase().split("\\s+");
        Map<String, Integer> map = new LinkedHashMap<>();

        for (String c : words) {
            map.put(c, map.getOrDefault(c, 0) + 1);
        }

        for(Map.Entry<Character, Integer> entry : map.entrySet())
        {
             if (entry.getKey().equals("big") || entry.getKey().equals("black")) 
             {
                System.out.println(entry.getKey() + " " + entry.getValue());
             }
        }

==============================================================================
//How many times word appears in a sentence.
06/04/2026 /
        import java.util.*; 

        String line = "Big black bug bit a big black dog on his big black nose";
        String[] words = line.toLowerCase().split("\\s+");

        Map<String,Integer> map = new LinkedHashMap<>();

        for (String c : words) {
            map.put(c, map.getOrDefault(c, 0) + 1);
        }

        for (Map.Entry<String,Integer> entry : map.entrySet()) 
        {
            System.out.println(entry.getKey()+" "+ entry.getValue());
        }

==============================================================================
06/04/2026 /
// AnagramUsingMap

            String str1 = "listen";
		 	String str2 = "silent";

	        Map<Character, Integer> map = new HashMap<>();

	        // If lengths are different, not anagrams
	        if (str1.length() != str2.length()) {
	            System.out.println("Not anagrams");
	            return;
	        }

	        // add str1 char in map one by one.
	        for (int i = 0; i < str1.length(); i++) {
	            char c = str1.charAt(i);
	            map.put(c, map.getOrDefault(c, 0) + 1);
	        }

	        // Reduce count using second string
	        for (int i = 0; i < str2.length(); i++) {
	            char c = str2.charAt(i);

	            if (!map.containsKey(c)) {
	                System.out.println("Not anagrams");
	                return;
	            }
                else
	            map.put(c, map.get(c) - 1);

	        if (map.get(c) == 0) 
	            map.remove(c);
	           
	        }

	        if (map.isEmpty())  
	            System.out.println("Strings are anagrams");
	        else 
	            System.out.println("Not anagrams");
	       
==============================================================================
* 02/04/2026 | 04/04/2026 |    
Duplicates in string

        String str = "deepak   ashokrao jagtap";
        str = str.replaceAll("\\s+","");
        Map<Character,Integer> map = new LinkedHashMap<>();
        for(int i=0;i<str.length();i++)
        {
            char c = str.charAt(i);
            if(map.containsKey(c))
            {
                map.put(c,map.get(c)+1);
            }
            else
            map.put(c,1);
        }
            for(Map.Entry<Character, Integer> entry : map.entrySet())
           {
               if(entry.getValue() > 1)
               {
                   System.out.println(entry.getKey() +" -> "+entry.getValue());
               }
           }

 ==============================================================================          
01/04/2026 | 04/04/2026 |
Print longest and 2nd longest word from String.

        String name = "my name is deepak";
        String[] words = name.split("\\s+");
        
        String longest = "";
        String secondLongest = "";
        
         for (int i = 0; i < words.length; i++)
         {
            if(words[i].length() > longest.length()){
                secondLongest = longest;
                longest = words[i];
            }
        }
        System.out.println(longest);
        System.out.println(secondLongest);

==============================================================================    
31/03/2026 | 04/04/2026 |
*
**
***

        for (int i=0; i<3; i++) 
		{
			for (int j=0; j<=i; j++) 
			{
				System.out.print("*");  *,  **,  ***
                System.out.print((char)('A'+j));  A,  AB,  ABC
			}
			System.out.println();
		}
==============================================================================
        for (int j=0; j<=i; j++)
        for (int j=i; j<5;  j++){      
==============================================================================
31/03/2026 | 04/04/2026 |

***
**
*

        for (int i=0; i<5; i++) 
		{
			for(int j=i; j<5; j++){
			{
				System.out.print("*");
			}
			System.out.println();
		}

==============================================================================
* 30/03/2026 | 04/04/2026 |
//Print arry in ascending and descending

        int arr[] = {8,4,5,9,7,0,1};

        for(int i=0;i<arr.length;i++)
        {
            for(int j=i+1;j<arr.length;j++)
            {
                if(arr[i]>arr[j]){   // o to 9
              //if(arr[i]<arr[j]){   // 9 to 0
                    int temp = arr[i];
                    arr[i]=arr[j];
                    arr[j]=temp;
                }
            }
        }
         for(int k=0;k<arr.length;k++)
        {
            System.out.print(arr[k] + " ");
        }
==============================================================================
28/03/2026 | 04/04/2026 |
Min Max array
//Asked in R system interview.

	int a[] = { 8, 4, 5, 0, 9, 7, 5, 5, 0, 1 };

		int min = a[0];
//      int Max = a[0]; // to find max nu.

        // why not int i = 0; in below for ()?
        // because a[0] is already stored in min

		for (int i = 1; i < a.length; i++) { 

//		    if (a[i] > max) to find Max nu.
			if (a[i] < min) 
			{
				min = a[i];
			}
		}
		System.out.println(min);
//      System.out.println(max);  //  to find max nu.
==============================================================================
27/03/2026 | 04/04/2026 |
String - Remove Duplicates

        String name = "aabdddess";   
        Set<Character> set = new LinkedHashSet<Character>();
        
        for(int i=0;i<name.length();i++)
        {
            char c = name.charAt(i);
            set.add(c);     
        }
        System.out.println(set);

Array = Remove duplicates
//Asked in R system interview.

        int arr[] = {8,4,5,0,9,7,5,5,0,1};
        Set<Integer> set = new LinkedHashSet<Integer>();
        for(int i=0;i<arr.length;i++)
            set.add(arr[i]);     
        System.out.println(set);

==============================================================================