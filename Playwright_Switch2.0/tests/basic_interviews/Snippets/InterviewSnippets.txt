//Palindrome?

        String name = "madam";
        String reverseName = "";
         
        for(int i=name.length()-1;i>=0;i--)
        {
          reverseName += name.charAt(i); 
        }

        if(name.equals(reverseName))
            System.out.println("Palindrome"); 
        else
            System.out.println("non-Palindrome"); 
        
   
============================================================================== 

Print all positive 

    int arr[] = {-10,-20,-30,0,10,20,30};
    
    for(int i=0;i<arr.length;i++)
        if(arr[i]<0)  
        arr[i] = -arr[i];
        
    for(int k=0;k<arr.length;k++)
         System.out.print(arr[k]+" ");

==============================================================================

//*Print most repitative character name and count.

  String name = "deepak test automation";
        name = name.replace(" ", ""); // remove spaces

        int maxCount = 0;
        char maxChar = ' ';

        for (int i = 0; i < name.length(); i++) {
            int count = 1;

            for (int j = i + 1; j < name.length(); j++) {
                if (name.charAt(i) == name.charAt(j)) {
                    count++;
                }
            }
            if (count > maxCount) {
                maxCount = count;
                maxChar = name.charAt(i);
            }
        }
        System.out.println("Most repeating character: " + maxChar);
        System.out.println("Repeating count: " + maxCount);

            ||::  ========= WITH MAP ========  ::||

        String name = "deepak test automation";
        name = name.replace(" ", "");  // remove spaces

        Map<Character, Integer> map = new LinkedHashMap<>();

        // Count frequency
        for (int i = 0; i < name.length(); i++) {
            char c = name.charAt(i);
            map.put(c, map.get(c, 0) + 1);
        }

        // Find most repeating character
        char maxChar = ' ';
        int maxCount = 0;

        for (Map.Entry<Character, Integer> entry : map.entrySet()) {
            if (entry.getValue() > maxCount) {
                maxCount = entry.getValue();
                maxChar = entry.getKey();
            }
        }

        System.out.println("Most repeating character: " + maxChar);
        System.out.println("Repeating count: " + maxCount);

============================================================================== 
//*Print 2nd most repitative character name and count.

        String name = "im deepak QA engineer";
        name = name.replaceAll("\\s+","");
        
        Map<Character, Integer> map = new LinkedHashMap<>();
        for(int i=0;i<name.length();i++){
            char c = name.charAt(i);
            
            if(map.containsKey(c))
                map.put(c,map.get(c)+1);
            else
                map.put(c,1);
        }
        int maxCount = 0, smaxCount = 0;
        char maxChar = ' ', smaxChar = ' ';
 
        for(Map.Entry<Character, Integer> entry : map.entrySet())
        {
            if(entry.getValue()>maxCount){
                smaxCount = maxCount;
                smaxChar = maxChar;
                
                maxCount = entry.getValue();
                maxChar = entry.getKey();
            }
             else if (entry.getValue() > smaxCount && entry.getValue() < maxCount) {
                smaxCount = entry.getValue();
                smaxChar = entry.getKey();
            }
        }
        System.out.println("Second most repeating character: " + smaxChar + " = " + smaxCount);
==============================================================================
//*Print Missing Number 

        int arr[] = {12, 14, 18};
        int missingNu = 0;

        int diff = Integer.MAX_VALUE;
        for(int i=0;i<arr.length-1;i++)
        {
           diff = Math.min(diff,arr[i+1]-arr[i]);
        }

        for (int i = 0; i < arr.length - 1; i++) {

            if (arr[i + 1] - arr[i] != diff) {
                missingNu = arr[i] + diff;
                break;
            }
        }

        System.out.println(missingNu);

==============================================================================

// frequency of characters in a string "aabdddess"
Output = {a=2, b=1, d=3, e=1, s=2}

import java.util.LinkedHashMap;
import java.util.Map;

        String str = "aabdddess";
        
        Map<Character,Integer> map = new LinkedHashMap<>();
        for(int i=0;i<str.length();i++)
        {
            char c = str.charAt(i);
            if(map.containsKey(c))
            {
                map.put(c,map.get(c)+1);
            }
            else
            map.put(c,1);
        }
         
           for(Map.Entry<Character, Integer> entry : map.entrySet())
           {
               System.out.println(entry.getKey() +" -> "+entry.getValue());
           }

==============================================================================
Duplicates in string

        String str = "deepak   ashokrao jagtap";
        str = str.replaceAll("\\s+","");
        Map<Character,Integer> map = new LinkedHashMap<>();
        for(int i=0;i<str.length();i++)
        {
            char c = str.charAt(i);
            if(map.containsKey(c))
            {
                map.put(c,map.get(c)+1);
            }
            else
            map.put(c,1);
        }
            for(Map.Entry<Character, Integer> entry : map.entrySet())
           {
               if(entry.getValue() > 1)
               {
                   System.out.println(entry.getKey() +" -> "+entry.getValue());
               }
           }

 ==============================================================================          
Print longest and 2nd longest word from String.

        String name = "my name is deepak";
        String[] words = name.split("\\s+");
        
        String longest = "";
        String secondLongest = "";
        
         for (int i = 0; i < words.length; i++)
         {
            if(words[i].length() > longest.length()){
                secondLongest = longest;
                longest = words[i];
            }
        }
        System.out.println(longest);
        System.out.println(secondLongest);

==============================================================================    

*
**
***
****
*****

    for (int row = 1; row <= 5; row++) 
		{
			for (int column = 1; column <= row; column++) 
			{
				System.out.print("*");
			}
			System.out.println();
		}
==============================================================================
        for (int row = 1; row <= 5; row++) 
        for (int row = 5; row >= 1; row--)       
==============================================================================

*****
****
***
**
*

        for (int row = 5; row >= 1; row--) 
		{
			for (int column = 1; column <= row; column++) 
			{
				System.out.print("*");
			}
			System.out.println();
		}

==============================================================================
//Print arry in ascending and descending

        int arr[] = {8,4,5,9,7,0,1};
        
        for(int i=1;i<=arr.length-1;i++)
        {
            for(int j=1;j<=arr.length-1;j++)
            {
                if(arr[i]<arr[j])  //0 1 4 5 7 9
                if(arr[i]>arr[j])  //9 7 5 4 1 0
                { 
                    int temp = arr[i];
                    arr[i]=arr[j];
                    arr[j]=temp;
                }
            }
        }
        for(int k=1;k<=arr.length-1;k++)
        {
            System.out.println(arr[k]);
        }
==============================================================================